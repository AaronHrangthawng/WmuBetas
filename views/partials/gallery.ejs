<% title = "Gallery – Exotic Epsilon" %>

<section class="container py-5">
  <div class="text-center mb-5">
    <h2 class="display-5 fw-bold" style="color: #7851A9;">Gallery</h2>
    <p class="lead">A collection of memories and moments from our chapter events.</p>
  </div>

  <div class="row g-4">
    <% images.forEach(img => { %>
      <div class="col-sm-6 col-md-4 col-lg-3">
        <img src="/images/galleryUploads/<%= img.file %>" class="img-fluid rounded shadow-sm" alt="<%= img.caption %>" />
        <p class="text-center small text-muted mt-1"><%= img.caption %></p>
      </div>
    <% }) %>
  </div>
</section>
